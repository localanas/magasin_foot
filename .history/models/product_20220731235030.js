class Category {
  constructor(ref, description, marque, prix) {
    this.ref = ref;
    this.description = description;
    this.marque = marque;
    this.prix = prix;
  }
}

export default Category;
